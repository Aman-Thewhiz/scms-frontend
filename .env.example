# Base URL for backend API namespace.
# Include the /api/v1 prefix because all API calls are routed under that namespace.
# Development example:
VITE_API_BASE_URL=http://localhost:4000/api/v1

# Production example:
# VITE_API_BASE_URL=https://your-domain.example/api/v1
